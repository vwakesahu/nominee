pragma language_version >= 0.26;
import CompactStandardLibrary;
export ledger sink: Field;
witness p(): JubjubPoint;
witness s(): JubjubScalar;
export circuit f(): [] { sink.write(disclose(jubjubPointX(ecMul(p(), s())))); }
