pragma language_version >= 0.26;
import CompactStandardLibrary;
export ledger sink: Bytes<32>;
export circuit f(x: Bytes<32>): [] {
  const h0 = x;
  sink.write(disclose(persistentHash<Field>(h0)));
}
